namespace Tests {
  /// <summary>
  /// Class DockerFile.
  /// </summary>
  public class DockerFile {
    /// <summary>
    /// Defines the test method ShouldCreate.
    /// </summary>
    [Fact]
    public void ShouldCreate() {
      //  Mock<IMapTextStream> fileWriter = new();
      //  //Mock<ITemplateGeneratorConfig<Key,Value>> config = new();
      //  var logger = new Mock<ILogger<TemplateGenerator>>();
      //  TemplateGenerator template = new(
      //    fileWriter.Object,
      //    config.Object,
      //    logger.Object);
      //  var code = template.Generate();
      //  code.Should().NotBeNull().And.NotBeEmpty();
    }
  }
}
